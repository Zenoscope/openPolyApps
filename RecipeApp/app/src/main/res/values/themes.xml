<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Bit603" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- add the following code-->
    <style name="Theme.Bit603.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#250fb9</item>
        <!--
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_screen_logo</item>
        -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/cog</item>
        <item name="postSplashScreenTheme">@style/Theme.Bit603</item>
    </style>
</resources>